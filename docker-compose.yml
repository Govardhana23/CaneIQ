version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
